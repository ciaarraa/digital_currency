# frozen_string_literal: true

RSpec.describe RunaProject do
  it "has a version number" do
    expect(RunaProject::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end

  it "tests the inclusion from the runa project" do
    
  end
end
